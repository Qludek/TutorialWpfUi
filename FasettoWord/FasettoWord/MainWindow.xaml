<Window x:Class="FasettoWord.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FasettoWord"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        x:Name="AppWindow"
        Icon="Images/Logo/logo-small.png"
        Title="Title of titles" Height="450" Width="800">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="{Binding ResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    
    <StackPanel>
        <TextBlock Text="Test" FontSize="40" />
        <TextBlock Text="Test" FontFamily="{StaticResource LatoThin}" FontSize="40" />
        <TextBlock Text="Test" FontFamily="{StaticResource LatoRegular}" FontSize="40" />
        <TextBlock Text="Test" FontFamily="{StaticResource LatoMedItalic}" FontSize="40" />

        <TextBlock Text="{Binding Test}" />
    </StackPanel>
</Window>
